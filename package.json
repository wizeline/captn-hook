{
  "name": "captnhook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "local": "ARC_LOCAL=true npx sandbox",
    "test": "standard --verbose | snazzy && echo \"Error: no test specified\" && exit 1",
    "run-standard": "standard --fix --verbose | snazzy"
  },
  "keywords": [],
  "author": "Eduardo Romero",
  "license": "ISC",
  "dependencies": {
    "@architect/architect": "^5.5.0"
  },
  "devDependencies": {
    "snazzy": "^8.0.0",
    "standard": "^12.0.1"
  }
}
